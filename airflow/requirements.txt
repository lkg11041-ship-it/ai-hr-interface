# ============================================================================
# Python Requirements for Airflow (일반 환경용)
# ============================================================================
#
# 이 파일은 인터넷 연결이 있는 일반 환경에서 Airflow를 실행하기 위한 의존성입니다.
# Oracle → PostgreSQL ETL 기능 포함
# ============================================================================

# ============================================================================
# Database Drivers
# ============================================================================
# PostgreSQL driver
psycopg2-binary>=2.9.9

# Oracle driver (Thin mode - Oracle Instant Client 불필요)
# python-oracledb는 cx_Oracle의 후속 버전으로 Pure Python 구현
# 폐쇄망 환경에서도 작동 (외부 C 라이브러리 의존성 없음)
oracledb>=2.0.0

# ============================================================================
# Data Processing
# ============================================================================
pandas>=2.1.4
numpy>=1.26.2

# ============================================================================
# Utility Libraries
# ============================================================================
python-dateutil>=2.8.2
pytz>=2023.3
